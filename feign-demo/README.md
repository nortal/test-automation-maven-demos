# Test-automation-maven-feign-demo

## How to:
* Run feign tests

`mvn clean test -Dtest=FeignDemoTest`

* Check the generated report

`build/allure-report/data/index.html`

### Additional information:

* Package is used for Back-End scenario testing
